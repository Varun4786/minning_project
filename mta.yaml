_schema-version: "3.2"
ID: appminningreport
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appminningreport-destination-content
  type: com.sap.application.content
  requires:
  - name: appminningreport-destination-service
    parameters:
      content-target: true
  - name: appminningreport-repo-host
    parameters:
      service-key:
        name: appminningreport-repo-host-key
  - name: appminningreport-uaa
    parameters:
      service-key:
        name: appminningreport-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appminningreport_html_repo_host
          ServiceInstanceName: appminningreport-html5-srv
          ServiceKeyName: appminningreport-repo-host-key
          sap.cloud.service: appminningreport
        - Authentication: OAuth2UserTokenExchange
          Name: appminningreport_uaa
          ServiceInstanceName: appminningreport-xsuaa-srv
          ServiceKeyName: appminningreport-uaa-key
          sap.cloud.service: appminningreport
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: appminningreport-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appminningreport-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appminningreport.zip
      name: appminningreport
      target-path: resources/
- name: appminningreport
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appminningreport-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appminningreport-destination-service
    service-plan: lite
- name: appminningreport-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appminningreport-xsuaa-srv
    service-plan: application
- name: appminningreport-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appminningreport-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
